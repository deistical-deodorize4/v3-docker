# ==========================================
# Pi Zero 2W Homelab
# All services in Docker
# ==========================================

TZ=Europe/Madrid
COMPOSE_PROJECT_NAME=pi-homelab

# --- PIHOLE ---
PIHOLE_WEBPASSWORD=changeme_pihole

# --- WIREGUARD (wg-easy v15) ---
# INIT_HOST must be your server's public IP or DDNS hostname
WG_HOST=vpn.example.com
INIT_ENABLED=true
INIT_HOST=vpn.example.com
INIT_PORT=51820
INIT_DNS=your_pi's_static_ip 

# --- PORTS ---
WG_UI_PORT=51821
